l = float(input('Digite o valor correspondente a base: '))
a = float(input('Digite o valor correspondente a altura: '))
area = l * a
print('Se a base é {}, a altura {} a área desse retângulo corresponde a {}'.format(l, a, area))

